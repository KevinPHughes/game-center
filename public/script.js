// This script can be used for any landing page specific JavaScript
// For now, it's empty as navigation is handled by HTML links.

document.addEventListener('DOMContentLoaded', () => {
    console.log("Landing page loaded.");
    // Add any landing page specific logic here if needed in the future
});
